# Elite Recovery OSINT Backend
# Simplified for reliable builds

FROM python:3.12-slim

# Set working directory
WORKDIR /app

# Install system dependencies + Playwright browser dependencies
RUN apt-get update && apt-get install -y \
    git \
    curl \
    wget \
    # Playwright/Chromium dependencies
    libnss3 \
    libnspr4 \
    libatk1.0-0 \
    libatk-bridge2.0-0 \
    libcups2 \
    libdrm2 \
    libdbus-1-3 \
    libxkbcommon0 \
    libatspi2.0-0 \
    libxcomposite1 \
    libxdamage1 \
    libxfixes3 \
    libxrandr2 \
    libgbm1 \
    libasound2 \
    libpango-1.0-0 \
    libcairo2 \
    fonts-liberation \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements first for caching
COPY requirements.txt .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Install ONLY the essential OSINT tools that work reliably
RUN pip install --no-cache-dir \
    sherlock-project \
    socialscan \
    holehe

# Install Playwright and Chromium browser for court search
RUN pip install --no-cache-dir playwright \
    && playwright install chromium --with-deps

# Copy application code
COPY . .

# Expose port
EXPOSE 8000

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8000/health || exit 1

# Run the application
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
